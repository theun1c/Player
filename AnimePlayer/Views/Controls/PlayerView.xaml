<UserControl x:Class="AnimePlayer.Views.Controls.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnimePlayer.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" 
             d:DesignWidth="800"
             Background="#121212">
    <StackPanel>
        <TextBlock Text="Player"
                Foreground="White"
                FontSize="28"
                HorizontalAlignment="Left"
                Margin="80, 20, 0, 20" RenderTransformOrigin="1.376,0.671"/>

        <StackPanel Orientation="Horizontal"
                 Margin="0,0,0, 10"
                 HorizontalAlignment="Center">

            <Border Width="400"
                 Height="200">


                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,2">
                        <GradientStop Color="#5bc3ff" Offset="0.0"/>
                        <GradientStop Color="#3aa0ff" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>



                <Border.Clip>
                    <RectangleGeometry RadiusX="10" 
                                    RadiusY="10"
                                    Rect="0,0,400,200"/>
                </Border.Clip>

                <!--сетка внутри плеера-->
                <Grid>
                    <MediaElement x:Name="mediaPlayer"
                                  Source="C:\Users\belov\Desktop\projects\AnimePlayer\AnimePlayer\Assets\VideoSourse\Bocchi the Rock!\Bocchi the Rock! - 1.mkv"
                                  LoadedBehavior="Manual"
                                  UnloadedBehavior="Stop"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10">
                        <Button Content="Play" Click="PlayButton_Click" Margin="5"/>
                        <Button Content="Pause" Click="PauseButton_Click" Margin="5"/>
                        <TextBox x:Name="episodeBox" Text="1"
                                 Height="30"
                                 Width="30"
                                 HorizontalContentAlignment="Center"
                                 VerticalContentAlignment="Center"/>
                    </StackPanel>
                </Grid>
                
            </Border>
             
        </StackPanel>

    </StackPanel>
</UserControl>

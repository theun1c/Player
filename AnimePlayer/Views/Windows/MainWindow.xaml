<Window x:Class="AnimePlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AnimePlayer.ViewModels"
        xmlns:local="clr-namespace:AnimePlayer"
        mc:Ignorable="d"
        Title="{Binding Path=Title}" 
        Height="450" 
        Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition ></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        

        <Border Grid.Row="0" Grid.RowSpan="2" Background="Red"></Border>
        <Border Grid.Row="2" Grid.ColumnSpan="2" Background="Blue"></Border>
        <Border Grid.Column="1" Background="Green"></Border>
        <Border Grid.Column="1" Grid.Row="1" Background="Purple"></Border>

        <TextBox Grid.Column="0" Grid.Row="0" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>
        
    </Grid>
</Window>
